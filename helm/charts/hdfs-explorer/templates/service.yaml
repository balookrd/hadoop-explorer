apiVersion: v1
kind: Service
metadata:
  name: {{ include "hdfs-explorer.fullname" . }}
  labels:
    {{- include "hdfs-explorer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hdfs-explorer.selectorLabels" . | nindent 4 }}
