apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hdfs-explorer.fullname" . }}
  labels:
    {{- include "hdfs-explorer.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  {{- $dbUrl := .Values.config.database.url | default "" -}}
  {{- if and (or (hasPrefix "sqlite" $dbUrl) (eq $dbUrl "")) (gt (.Values.replicaCount | int) 1) }}
  replicas: 1
  {{- else }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "hdfs-explorer.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "hdfs-explorer.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "hdfs-explorer.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: HDFS_CONFIG_PATH
              value: "/etc/hdfs-explorer/config.yaml"
            - name: HDFS_CLUSTERS_PATH
              value: "/etc/hdfs-explorer/clusters.yaml"
            {{- if .Values.config.database.url }}
            - name: HDFS_DATABASE_URL
              value: {{ .Values.config.database.url | quote }}
            {{- end }}
            - name: HDFS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "hdfs-explorer.fullname" . }}-secrets
                  key: jwt-secret
            - name: HDFS_LDAP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "hdfs-explorer.fullname" . }}-secrets
                  key: ldap-password
            {{- if .Values.secrets.databasePassword }}
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "hdfs-explorer.fullname" . }}-secrets
                  key: database-password
            {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: app-config
              mountPath: /etc/hdfs-explorer/config.yaml
              subPath: config.yaml
            - name: app-config
              mountPath: /etc/hdfs-explorer/clusters.yaml
              subPath: clusters.yaml
            {{- if .Values.kerberos.enabled }}
            - name: app-config
              mountPath: /etc/krb5.conf
              subPath: krb5.conf
            {{- end }}
            {{- if .Values.customLdapCaCert }}
            - name: app-config
              mountPath: /etc/ssl/certs/ldap-ca.crt
              subPath: ldap-ca.crt
            {{- end }}
            {{- if .Values.secrets.kerberosKeytabBase64 }}
            - name: keytab-volume
              mountPath: /etc/security/keytabs
              readOnly: true
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - name: data-volume
              mountPath: /app/data
            {{- end }}
            - name: tmp-volume
              mountPath: /tmp
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: data-volume
          persistentVolumeClaim:
            claimName: {{ include "hdfs-explorer.fullname" . }}-data
        {{- end }}
        - name: tmp-volume
          emptyDir: {}
        - name: app-config
          configMap:
            name: {{ include "hdfs-explorer.fullname" . }}-config
        {{- if .Values.secrets.kerberosKeytabBase64 }}
        - name: keytab-volume
          secret:
            secretName: {{ include "hdfs-explorer.fullname" . }}-secrets
            items:
              - key: hdfs-explorer.keytab
                path: hdfs-explorer.keytab
                mode: 0400
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
