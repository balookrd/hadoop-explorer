{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "hdfs-explorer.fullname" . }}
  labels:
    {{- include "hdfs-explorer.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "hdfs-explorer.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - protocol: TCP
          port: {{ .Values.service.targetPort }}
      {{- if .Values.networkPolicy.ingress.from }}
      from:
        {{- toYaml .Values.networkPolicy.ingress.from | nindent 8 }}
      {{- end }}
  egress:
    {{- if .Values.networkPolicy.egress.allowDns }}
    # DNS queries (TCP and UDP)
    - to: []
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- end }}
    {{- if .Values.networkPolicy.egress.allowKerberos }}
    # Kerberos KDC
    - to: []
      ports:
        - protocol: UDP
          port: 88
        - protocol: TCP
          port: 88
        - protocol: TCP
          port: 749
    {{- end }}
    {{- if .Values.networkPolicy.egress.allowLdap }}
    # LDAP and LDAPS
    - to: []
      ports:
        - protocol: TCP
          port: 389
        - protocol: TCP
          port: 636
    {{- end }}
    {{- if .Values.networkPolicy.egress.allowHdfs }}
    # WebHDFS NameNodes & DataNodes
    - to: []
      ports:
        - protocol: TCP
          port: 9870
        - protocol: TCP
          port: 9864
        - protocol: TCP
          port: 50070
        - protocol: TCP
          port: 50075
    {{- end }}
    {{- if .Values.networkPolicy.egress.customRules }}
    {{- toYaml .Values.networkPolicy.egress.customRules | nindent 4 }}
    {{- end }}
{{- end }}
